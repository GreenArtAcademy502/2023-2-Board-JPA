spring:
  datasource:
    url: jdbc:mariadb://localhost/board2023
    username: root
    password: green502
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    generate-ddl: false

springdoc:
  swagger-ui:
    path: /swagger.html